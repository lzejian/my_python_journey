name: 每日基金策略 (含记忆功能)

on:
  schedule:
    # 北京时间 14:10 = UTC时间 06:10
    # 这样就算排队 20 分钟，你也能在 14:30 左右收到，有充足的时间去支付宝操作
    - cron: '10 6 * * 1-5'
  
  # 允许手动点按钮触发测试
  workflow_dispatch:

jobs:
  run_strategies:
    runs-on: ubuntu-latest
    
    steps:
    - name: 1. 拉取仓库代码
      uses: actions/checkout@v3
      
    - name: 2. 设置 Python 环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 3. 安装依赖库
      run: pip install akshare requests pandas

    - name: 4. 运行纳指策略
      continue-on-error: true 
      run: python smart_strategy.py
      
    # 👇👇👇 新增这一步：强行等待 15 秒 👇👇👇
    - name: ⏸️ 等待消息推送 (防止手表吞消息)
      run: sleep 15

    - name: 5. 运行黄金策略
      continue-on-error: true
      run: python gold_strategy.py

    - name: 6. 【核心】保存记忆 (Git Push)
      run: |
        # 配置一个虚拟的 git 用户
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "actions@github.com"
        
        # 检查有没有 json 文件变化
        git add *.json
        
        # 如果有变化，就提交；没变化(比如周末)就跳过
        if git diff --staged --quiet; then
          echo "📅 数据无变化，无需保存"
        else
          git commit -m "🤖 自动更新策略记忆数据 [skip ci]"
          git push
          echo "✅ 记忆数据已保存回仓库"
        fi
